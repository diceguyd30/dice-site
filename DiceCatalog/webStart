
#!/bin/bash

#Invoke the Forever module (to START our Node.js server).
DIR="$( cd "$( dirname "$0" )" && pwd )"
$DIR/node_modules/forever/bin/forever \
        start \
        -al forever.log \
	-ao output.log \
        -ae error.log \
	$DIR/index.js


